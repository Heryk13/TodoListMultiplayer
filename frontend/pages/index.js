export default function Home() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>🚀 Next.js + FastAPI + Postgres Stack</h1>
      <p>Frontend is running correctly!</p>
      <p>Try fetching from the backend: <code>/api/</code></p>
    </main>
  );
}
